<UserControl x:Class="SteamTools.UI.Views.ProfileDataFetcherView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:SteamTools.UI.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="375"
             d:DesignWidth="480"
             d:DataContext="{d:DesignInstance viewModels:ProfileDataFetcherViewModel}">
    <Grid Background="#202020">
        <Grid.RowDefinitions>
            <RowDefinition Height="70" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBox Style="{StaticResource SteamIDInputTextBox}" Background="White"
                         Height="40" Margin="15 15 0 0" Padding="15 0 15 0"
                         Text="{Binding EnteredText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Tag="Enter Steam ID">
                    <TextBox.InputBindings>
                        <KeyBinding Command="{Binding GetProfileDetailsCommand}" Key="Enter" />
                    </TextBox.InputBindings>
                </TextBox>
            </StackPanel>
            <StackPanel Grid.Column="1">
                <Button Style="{StaticResource LookupButton}" FontSize="18"
                        Height="40" Margin="0 15 15 0" Content="Lookup" Command="{Binding GetProfileDetailsCommand}" />
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="360" />
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Image Source="{Binding ProfileDetails.PlayerSummaries.AvatarFull}" VerticalAlignment="Top"
                       HorizontalAlignment="Center" Width="92" Height="92" />
            </Grid>
            <StackPanel Grid.Column="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40" />
                        <RowDefinition Height="40" />
                        <RowDefinition Height="40" />
                        <RowDefinition Height="40" />
                        <RowDefinition Height="40" />
                        <RowDefinition Height="40" />
                    </Grid.RowDefinitions>
                    <TextBox Grid.Row="0" VerticalAlignment="Center" Style="{StaticResource USTextBox}"
                             TextAlignment="Center" IsReadOnly="True"
                             Text="{Binding ProfileDetails.SteamID, Mode=OneWay}" />
                    <TextBox Grid.Row="1" VerticalAlignment="Center" Style="{StaticResource USTextBox}"
                             TextAlignment="Center" IsReadOnly="True"
                             Text="{Binding ProfileDetails.SteamID3, Mode=OneWay}" />
                    <TextBox Grid.Row="2" VerticalAlignment="Center" Style="{StaticResource USTextBox}"
                             TextAlignment="Center" IsReadOnly="True"
                             Text="{Binding ProfileDetails.SteamID32, Mode=OneWay}" />
                    <TextBox Grid.Row="3" VerticalAlignment="Center" Style="{StaticResource USTextBox}"
                             TextAlignment="Center" IsReadOnly="True"
                             Text="{Binding ProfileDetails.SteamID64, Mode=OneWay}" />
                    <TextBox Grid.Row="4" VerticalAlignment="Center" Style="{StaticResource USTextBox}"
                             TextAlignment="Center" IsReadOnly="True"
                             Text="{Binding ProfileDetails.ProfileCustomUrl, Mode=OneWay}" />
                    <TextBox Grid.Row="5" VerticalAlignment="Center" Style="{StaticResource USTextBox}"
                             TextAlignment="Center" IsReadOnly="True"
                             Text="{Binding ProfileDetails.ProfilePermanentUrl, Mode=OneWay}" />
                </Grid>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>