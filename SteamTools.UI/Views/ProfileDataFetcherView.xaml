<UserControl x:Class="SteamTools.UI.Views.ProfileDataFetcherView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:SteamTools.UI.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="275"
             d:DesignWidth="480"
             d:DataContext="{d:DesignInstance viewModels:ProfileDataFetcherViewModel}">
    <Grid Background="#202020">
        <Grid.RowDefinitions>
            <RowDefinition Height="70" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBox Style="{StaticResource USTextBox}" Background="{StaticResource ControlButtonMouseOverColor}"
                         Height="40" Margin="15 15 5 0"
                         Text="{Binding EnteredText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            <StackPanel Grid.Column="1">
                <Button Foreground="{StaticResource TextColor}" Style="{StaticResource MWButton}" FontSize="30"
                        Height="40" Margin="5 15 15 0" Content="→" Command="{Binding GetProfileDetailsCommand}" />
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" />
            <StackPanel Grid.Column="1" />
        </Grid>
    </Grid>
</UserControl>